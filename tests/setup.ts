// Test setup file
import { config } from '@vue/test-utils'

// Global test configuration for Vue components
config.global.stubs = {
  // Stub out any global components that might cause issues in tests
}